# Template-oracle declaration (tools.repository spec 007 ADR 001): this template
# is the layout oracle for the "docs" primary class — its presence activates that
# class's layout verification fleet-wide.
class: docs
